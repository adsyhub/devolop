# 2026-09-08 修正记录与验收范围

本轮已直接修改代码、界面、测试与真实工作区数据。原 `CURRENT_IMPLEMENTATION_REVIEW_2026-09-07.md` 是历史快照，不再代表当前启动状态。不能把本轮修复等同于原 IMP-01–IMP-20 所有扩展功能及真实内容均已完成。

## 已落地的修正

| 问题 | 修正后的行为 | 验证位置 |
|---|---|---|
| 启动、重复路由与重复写入 | 恢复 Python/JavaScript/Schema 可加载；任务入口统一处理，写操作执行一次；默认管理凭证和题库实例 ID 持久化策略落地 | `test_server_v1.py`、`test_remote_boundary.py`、发布检查 |
| 保存、交卷与恢复 | 批次事务、版本冲突、请求回执、暂停/继续、过期提交；笔记恢复保留草稿原始基准版本，不能刷新后绕过冲突检查 | 会话、浏览器及工作流回归 |
| 未答与作文统计 | 评分口径 v3；空作文为未答、非空作文待评阅，两者均不进入客观题分母；旧结果保留原记录 | `test_session_reliability.py`、`test_workflow_acceptance.py` |
| 复核、完整度与清单 | 来源必须绑定精确清单项；完整卷依赖独立签署的结构、逐页签署及区域处置；新增结构编辑/读取/签署界面；发布前重查文件哈希、权限、结构和页版本 | `test_content_review.py`、`test_review_browser.py` |
| 部分内容无法明确发布 | 可在完整来源基线内签署具体题目、物理页和缺失原因，按该范围生成 PARTIAL 版本；不能因此声明整卷完整或开启 MOCK | `test_content_review.py` |
| OCR 与渲染缓存 | 缓存单独存放；核对 PDF/图片哈希、模型配置及页码角色；缓存命中也记录尝试；保留旧输出与原始识别产物；拒绝覆盖签署输出 | `test_ocr_pipeline.py` |
| 后台任务只是状态壳 | 实际导入、探测、渲染、OCR、备份；租约、心跳、取消检查点、失败重试、过期租约恢复；HTTP 请求不承担长时间制作工作 | `test_jobs.py`、`test_management_browser.py` |
| 媒体记录与历史 | 原子写入、目录约束、实际尺寸与来源记录；已停用内容的旧会话可读取自身媒体，不能读取任意私人素材 | `test_assets_audio.py`、`test_workflow_acceptance.py` |
| 音频与记述 | 真实音频探测、来源绑定、片段校验、播放位置恢复及片段结束停止；可维护练习 rubric、自评和人工评阅，评阅不改客观得分 | 音频、工作流与管理浏览器回归 |
| 解析与翻译互相覆盖 | 按题版本、种类、语言分别冻结；提交前不可读取；新修订不改旧复盘；撤回翻译不隐藏解析 | `test_workflow_acceptance.py` |
| 检索、历史与学习统计 | 检索投影及游标分页；收藏/错题不再截断前 200 项；固定版本的组题预览与幂等开始；首答、重练、未答、目标与日历；补建旧会话的统计事实 | 工作流回归及性能报告 |
| 纠错丢失版本归属 | 从会话发起的纠错绑定该次题卷版本；处理流转保留事件，关闭前要求存在后续可用修订 | 内容纠错与工作流回归 |
| 备份、诊断及安装 | 学习包/完整制作包、校验、恢复至新目录、管理下载；保守缓存清理预览、诊断、统一工作区路径；可重复执行源码与 wheel 检查 | 备份回归、管理浏览器、`scripts/verify_release.py` |

## 真实资料与数据库的实际变化

- 已把 2023 理科及五份 2024 来源分别绑定到对应的六个清单 ID，数学 Course 1 / Course 2 分开绑定。
- 重新查看真实 PDF 后，确认 2023 理科物理第 41 页只有页眉页脚，没有原录入的 `bio-q-01`、`bio-q-02` 正文。
- 该页原文件已归档至 `work/2023-2-science/revisions/20260908T002243Z/`；页面文件和数据库新增草稿已纠正为非题目页，并明确保留待复核问题。
- 没有把原两题擅自移动到其他页，也没有签署整卷或发布替代版本。
- 正式库仍保留 **1 个发布版本、6 个会话**。数据库迁移至 **v8**，新增 **1 个纠正草稿、195 条历史作答统计事实**；完整性检查 `ok`，外键问题为 0。
- 真实卷继续保持 `REVIEW_REQUIRED`，阻止新的误导性练习；页面和结构的人工签署仍未完成。

证据：[真实来源纠正](evidence/real-source-correction-2026-09-08.json)、[正式库检查](evidence/formal-library-after-fixes-2026-09-08.json)。

完整正式工作区备份已校验并实际恢复至临时目录：6 份来源、6 个会话、第 41 页纠正草稿和旧结果均可读取，恢复库 v8 完整性检查通过。备份位于 `library/backups/eju_backup_2026-09-08T00-32-03-522877Z.zip`，约 105 MB，证据见 [完整恢复演练](evidence/full-formal-restoration-2026-09-08.json)。演练没有替换正式目录。

## 验收记录

最终自动验收为 **105 passed in 64.77s**，记录保存于 [完整发布检查](evidence/final-fixes-release-2026-09-08.json)，对应 JUnit 位于同目录。Schema 同步、Python/JavaScript 语法、全部测试、Chromium、v0.2.0 wheel 构建、临时安装环境中的离线资源加载与跨工作目录启动均通过。源码、测试和资源在检查前后哈希一致。安装验证使用继承系统依赖的临时环境，不能代替 Windows 实机或完全无预装依赖的安装验证。

性能报告见 [一万题基准](evidence/learning-benchmark-2026-09-08.json)。其中检索 p95 约 40 ms、历史 p95 约 7 ms、批量保存 p95 约 5.5 ms，均为该次环境的数据库调用耗时；不代表浏览器首屏或 HTTP 延迟承诺。

## 保留的边界和后续工作

1. **真实内容验收**：2023 整卷仍需逐页核对、核对两道生物题的真实出处、签署独立题序和答案基线。第 41 页草稿修正不是整套卷验收。
2. **外部材料**：五份 2024 题卷仍缺独立答案，日本语还缺真实音频；没有使用合成题、推测答案或伪音频填补。
3. **严格模拟考试**：日本语严格 MOCK 的播放次数、跳转、完整时序与规则证据仍未全部验收，当前不开放这种模式。练习播放器与片段功能已实现。
4. **生产扩展**：显式部分范围发布已实现；编辑器支持块增删、答案类型、数字格、选项和区域调整。区域拆分合并的高级编辑、批量修订等原规格仍需继续实现。
5. **大规模体验和运维扩展**：已有数据库基准和基础键盘处理，但 Windows 实机、移动端/读屏完整矩阵、浏览器性能预算，以及日志/OCR 原始产物的完整保留策略尚未全部验收。当前清理仅针对可再生成的渲染缓存。

上述项目不因本轮测试通过而自动视为完成。旧计划的需求继续保留；本记录只声明已落地且有证据支持的修正。

## 启动与回退

在项目目录运行 `python -m eju_bank serve`。启动输出会显示本机管理凭证，在页面连接入口输入后访问复核和管理功能。代码已更新时需重启旧服务。

完整检查：`python scripts/verify_release.py --output docs/evidence/final-fixes-release-2026-09-08.json`。

本轮修改前代码与数据库备份在项目同级 `eju-fixes-backup-20260908T000551Z.zip`；真实资料修正前数据库在 `library/backups/before-source-correction-20260908T002243Z.db`；v8 迁移前数据库在 `library/backups/before-schema8-annotations.db`。回退应使用独立目录验证，避免覆盖正在运行的库。
